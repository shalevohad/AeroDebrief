<UserControl x:Class="AeroDebrief.UI.Views.Analytics.SignalQualityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="600"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <Border Grid.Row="0" Background="#F5F5F5" Padding="12,8" Margin="0,0,0,8">
            <TextBlock Text="Signal Quality" 
                     FontSize="16" 
                     FontWeight="SemiBold"
                     Foreground="#333333"/>
        </Border>

        <!-- Canvas for Signal Quality Chart -->
        <Border Grid.Row="1" Background="White">
            <Canvas x:Name="SignalCanvas" Background="Transparent"/>
        </Border>

        <!-- Placeholder -->
        <Border Grid.Row="1" 
               Background="Transparent"
               Visibility="{Binding HasData, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <TextBlock Text="No signal quality data available"
                     FontSize="14"
                     Foreground="#999999"
                     TextAlignment="Center"
                     HorizontalAlignment="Center"
                     VerticalAlignment="Center"/>
        </Border>
    </Grid>
</UserControl>
