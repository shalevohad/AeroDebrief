<Window x:Class="AeroDebrief.UI.Windows.ConversionProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:fa="http://schemas.fontawesome.io/icons/"
        Title="Converting Recording File" 
        Width="500" 
        Height="280"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="True"
        Topmost="True"
        Background="{DynamicResource WindowBackgroundBrush}">
    
    <Window.Resources>
        <!-- Modern styles -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        
        <Style x:Key="SubtitleTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        
        <Style x:Key="MonospaceTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas, Courier New"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedTextBrush}"/>
            <Setter Property="FontSize" Value="11"/>
        </Style>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title Icon + Text -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <fa:ImageAwesome Icon="Database" 
                            Width="24" 
                            Height="24" 
                            Foreground="{DynamicResource AccentBrush}"
                            VerticalAlignment="Center"
                            Margin="0,0,10,0"/>
            <TextBlock Text="Converting Recording File" 
                      Style="{StaticResource TitleTextStyle}"
                      VerticalAlignment="Center"/>
        </StackPanel>
        
        <!-- File Name -->
        <TextBlock Grid.Row="1" 
                  x:Name="FileNameText"
                  Text="Converting: recording.adb"
                  Style="{StaticResource SubtitleTextStyle}"
                  TextTrimming="CharacterEllipsis"
                  Margin="0,0,0,20"/>
        
        <!-- Main Content Area -->
        <Grid Grid.Row="2" Margin="0,0,0,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Stage Message -->
            <TextBlock Grid.Row="0"
                      x:Name="StageText"
                      Text="Initializing conversion..."
                      FontSize="14"
                      Margin="0,0,0,10"/>
            
            <!-- Detailed Status Message -->
            <TextBlock Grid.Row="1"
                      x:Name="DetailText"
                      Text="Processing: 0 packets"
                      Style="{StaticResource MonospaceTextStyle}"
                      Margin="0,0,0,8"/>
            
            <!-- Speed and ETA -->
            <Grid Grid.Row="2">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0"
                          x:Name="SpeedText"
                          Text="Speed: 0 pkt/sec"
                          Style="{StaticResource MonospaceTextStyle}"/>
                
                <TextBlock Grid.Column="1"
                          x:Name="EtaText"
                          Text="ETA: --:--"
                          Style="{StaticResource MonospaceTextStyle}"/>
            </Grid>
        </Grid>
        
        <!-- Progress Bar -->
        <ProgressBar Grid.Row="3" 
                    x:Name="ProgressBar"
                    Height="8"
                    Minimum="0"
                    Maximum="100"
                    Value="0"
                    IsIndeterminate="False"
                    Margin="0,0,0,8"/>
        
        <!-- Percentage Text -->
        <TextBlock Grid.Row="4"
                  x:Name="PercentText"
                  Text="0%"
                  HorizontalAlignment="Center"
                  Style="{StaticResource MonospaceTextStyle}"
                  FontSize="12"
                  Margin="0,0,0,15"/>
        
        <!-- Warning/Info Messages -->
        <Border Grid.Row="5"
                x:Name="InfoPanel"
                Background="{DynamicResource InfoBackgroundBrush}"
                BorderBrush="{DynamicResource InfoBorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="10"
                Margin="0,0,0,10"
                Visibility="Collapsed">
            <StackPanel Orientation="Horizontal">
                <fa:ImageAwesome Icon="InfoCircle" 
                                Width="16" 
                                Height="16"
                                Foreground="{DynamicResource InfoForegroundBrush}"
                                VerticalAlignment="Top"
                                Margin="0,0,8,0"/>
                <TextBlock x:Name="InfoText"
                          Text="This may take several minutes for large files."
                          TextWrapping="Wrap"
                          Foreground="{DynamicResource InfoForegroundBrush}"/>
            </StackPanel>
        </Border>
        
        <!-- Cancel Button -->
        <Button Grid.Row="6"
                x:Name="CancelButton"
                Content="Cancel"
                Width="100"
                Height="32"
                HorizontalAlignment="Right"
                Click="CancelButton_Click"
                Style="{DynamicResource ModernSecondaryButton}"/>
    </Grid>
</Window>
